---
phase: 02-core-agents
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-arc-executor.md
  - agents/gsd-arc-planner.md
autonomous: true
requirements:
  - AMOD-01
  - AMOD-02
  - AMOD-03

must_haves:
  truths:
    - "gsd-arc-executor adds @gsd-decision tags for significant design choices during execution"
    - "gsd-arc-executor removes completed @gsd-todo tags from files it touches"
    - "gsd-arc-planner reads @gsd-tags as planning input when code-based mode is enabled"
    - "Both wrapper agents check arc.enabled config and fall back to upstream behavior when false"
    - "Upstream agent files gsd-executor.md and gsd-planner.md remain completely unmodified"
  artifacts:
    - path: "agents/gsd-arc-executor.md"
      provides: "ARC-aware executor wrapper with tag maintenance obligations"
      contains: "name: gsd-arc-executor"
    - path: "agents/gsd-arc-planner.md"
      provides: "ARC-aware planner wrapper with code-based input mode"
      contains: "name: gsd-arc-planner"
  key_links:
    - from: "agents/gsd-arc-executor.md"
      to: "gsd-tools.cjs config-get arc.enabled"
      via: "Bash at startup"
      pattern: "config-get arc.enabled"
    - from: "agents/gsd-arc-planner.md"
      to: "gsd-tools.cjs config-get arc.enabled"
      via: "Bash at startup"
      pattern: "config-get arc.enabled"
    - from: "agents/gsd-arc-planner.md"
      to: ".planning/prototype/CODE-INVENTORY.md"
      via: "Read tool when code-first mode active"
      pattern: "CODE-INVENTORY.md"
---

<objective>
Create the ARC wrapper agents (gsd-arc-executor and gsd-arc-planner) that extend upstream agent behavior with config-gated ARC annotation obligations.

Purpose: These wrappers enable ARC tag maintenance during execution (adding @gsd-decision, removing completed @gsd-todo) and code-based planning input (reading @gsd-tags alongside requirements docs) -- all gated by config so non-ARC users are unaffected.
Output: agents/gsd-arc-executor.md, agents/gsd-arc-planner.md
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@agents/gsd-executor.md
@agents/gsd-planner.md
@get-shit-done/references/arc-standard.md

<interfaces>
Upstream agents that wrappers extend (Option A: self-contained prose, no runtime reads).

From agents/gsd-executor.md: name=gsd-executor, tools=Read/Write/Edit/Bash/Grep/Glob, permissionMode=acceptEdits, color=yellow, has commented hooks pattern in frontmatter. Executor behavior: atomic commits per task, deviation handling, checkpoint protocols, state management, SUMMARY.md creation.

From agents/gsd-planner.md: name=gsd-planner, tools=Read/Write/Bash/Glob/Grep/WebFetch/mcp__context7__*, color=green, has commented hooks pattern. Planner behavior: task breakdown, dependency analysis, goal-backward verification, PLAN.md with frontmatter.

Config gating pattern (from 02-RESEARCH.md):
  ARC_ENABLED=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get arc.enabled 2>/dev/null || echo "false")

Wrapper delegation approach (Option A -- recommended by research):
Wrappers are self-contained prose files. ARC-specific behavior is additive.
The wrapper carries its own complete instructions -- no runtime reads of upstream agent files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gsd-arc-executor wrapper agent</name>
  <files>agents/gsd-arc-executor.md</files>
  <read_first>
    - agents/gsd-executor.md (FULL read -- understand all execution behavior the wrapper must replicate)
    - get-shit-done/references/arc-standard.md (tag types for @gsd-decision and @gsd-todo)
    - .planning/phases/02-core-agents/02-RESEARCH.md (Pattern 3: Config Gating, Pattern 4: Wrapper Delegation)
  </read_first>
  <action>
Create `agents/gsd-arc-executor.md` (per D-11, D-13, D-14):

YAML frontmatter (wrapped in triple-dash delimiters):
  name: gsd-arc-executor
  description: ARC-aware executor that extends gsd-executor with annotation obligations -- adds @gsd-decision tags and removes completed @gsd-todo tags. Spawned by workflow commands when arc.enabled is true.
  tools: Read, Write, Edit, Bash, Grep, Glob
  permissionMode: acceptEdits
  color: yellow
  # hooks:
  #   PostToolUse:
  #     - matcher: "Write|Edit"
  #       hooks:
  #         - type: command
  #           command: "npx eslint --fix $FILE 2>/dev/null || true"

CRITICAL: The commented `# hooks:` block MUST be inside the frontmatter. Do NOT include a `skills:` field.

Role block (`<role>` XML tag):
"You are the GSD ARC executor -- you execute plans exactly like the standard GSD executor, with additional ARC annotation obligations. You follow ALL standard executor behavior: atomic commits, deviation handling, checkpoint protocols, state management, SUMMARY.md creation."

Additional ARC obligations (when arc.enabled is true):
1. When you make a significant design choice during execution, add a `@gsd-decision` tag in a comment above the relevant code
2. When you complete work that fulfills a `@gsd-todo` tag in a file you touch, remove that @gsd-todo comment line
3. Follow arc-standard.md syntax for all tags you add

MUST include: "**ALWAYS use the Write tool to create files** -- never use `Bash(cat << 'EOF')` or heredoc commands for file creation."

Project context block (`<project_context>` XML tag): Same as gsd-executor -- read CLAUDE.md at startup.

Execution flow (`<execution_flow>` XML tag):

Step 0 `check_arc_config` (FIRST step, before anything else):
Check if ARC mode is enabled by running:
  ARC_ENABLED=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get arc.enabled 2>/dev/null || echo "false")
If ARC_ENABLED is "false" or empty, skip all ARC-specific obligations and behave as a standard executor. Only apply ARC obligations when ARC_ENABLED is "true".

Remaining steps: Replicate the FULL execution flow from gsd-executor.md in prose (Option A delegation). Include ALL of these behaviors in the wrapper's own instructions:
- Load project state (read STATE.md, identify phase/plan)
- Mandatory initial read (if files_to_read block present, read all listed files)
- Task execution loop (read task, execute action, verify, commit atomically)
- Atomic commits per task (feat/fix/refactor prefix)
- Deviation handling (log deviations, continue if possible)
- Checkpoint protocols (pause at checkpoint:human-verify, checkpoint:decision, checkpoint:human-action)
- State management (update STATE.md progress)
- SUMMARY.md creation (write summary with frontmatter on completion)

Within the task execution loop, ADD the ARC obligations (only when ARC_ENABLED is "true"):
- For each file the executor modifies, scan for @gsd-todo tags. If a @gsd-todo describes work that this task just completed, REMOVE the entire @gsd-todo comment line.
- When making significant design choices (where multiple reasonable approaches existed), add a `@gsd-decision [description] | rationale: [why this approach]` comment tag above the relevant code.
- Follow ARC comment anchor rule: comment token must be first non-whitespace on the line.

Constraints block (`<constraints>` XML tag):
1. When arc.enabled is false, behave IDENTICALLY to standard gsd-executor -- no ARC behavior
2. Only remove @gsd-todo tags that are genuinely completed by the current task's work
3. Only add @gsd-decision tags for genuine design choices, not routine implementation
4. Follow arc-standard.md syntax exactly for all added tags
5. Do not modify gsd-executor.md or any upstream files
6. ARC obligations are secondary to plan execution -- complete the task first, then handle tags
7. Use Write tool for all file creation, never heredoc via Bash
  </action>
  <verify>
    <automated>cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node --test tests/agent-frontmatter.test.cjs 2>&1 | grep -E "gsd-arc-executor|pass|fail" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-arc-executor.md exists
    - File contains `name: gsd-arc-executor` in frontmatter
    - File contains `tools: Read, Write, Edit, Bash, Grep, Glob`
    - File contains `permissionMode: acceptEdits`
    - File contains `# hooks:` commented section in frontmatter
    - File contains the phrase `never use \`Bash(cat << 'EOF')\` or heredoc`
    - File contains `config-get arc.enabled` (config gating per D-14)
    - File contains `|| echo "false"` (graceful fallback for missing config key)
    - File contains `@gsd-decision` (adds decision tags per AMOD-01)
    - File contains `@gsd-todo` (removes completed todos per AMOD-01)
    - File contains `arc-standard.md` (follows ARC syntax)
    - File contains `SUMMARY.md` (produces summary like standard executor)
    - File contains `atomic` or `commit` (atomic commit behavior)
    - File does NOT contain `skills:` in frontmatter
    - agents/gsd-executor.md is UNMODIFIED (verify with git diff)
    - agent-frontmatter.test.cjs does not report new failures for gsd-arc-executor
  </acceptance_criteria>
  <done>gsd-arc-executor.md passes frontmatter tests, checks arc.enabled config, adds @gsd-decision tags, removes completed @gsd-todo tags, and falls back to standard behavior when disabled</done>
</task>

<task type="auto">
  <name>Task 2: Create gsd-arc-planner wrapper agent</name>
  <files>agents/gsd-arc-planner.md</files>
  <read_first>
    - agents/gsd-planner.md (FULL read -- understand all planning behavior the wrapper must replicate)
    - agents/gsd-arc-executor.md (just created -- verify config gating pattern consistency)
    - get-shit-done/references/arc-standard.md (tag types for planning input)
    - .planning/phases/02-core-agents/02-RESEARCH.md (Pattern 3: Config Gating, Pattern 4: Wrapper Delegation)
  </read_first>
  <action>
Create `agents/gsd-arc-planner.md` (per D-12, D-13, D-14):

YAML frontmatter (wrapped in triple-dash delimiters):
  name: gsd-arc-planner
  description: ARC-aware planner that extends gsd-planner to accept @gsd-tags as planning input alongside or instead of requirements docs. Spawned by workflow commands when arc.enabled is true.
  tools: Read, Write, Bash, Glob, Grep, WebFetch, mcp__context7__*
  color: green
  # hooks:
  #   PostToolUse:
  #     - matcher: "Write|Edit"
  #       hooks:
  #         - type: command
  #           command: "npx eslint --fix $FILE 2>/dev/null || true"

Note: tools list matches gsd-planner.md exactly (includes WebFetch, mcp__context7__*) since this wrapper extends planner behavior. Do NOT include a `skills:` field.

Role block (`<role>` XML tag):
"You are the GSD ARC planner -- you create executable phase plans exactly like the standard GSD planner, with additional capability to read @gsd-tags as planning input. You follow ALL standard planner behavior: task breakdown, dependency analysis, goal-backward verification, PLAN.md generation with frontmatter."

Additional ARC capability (when arc.enabled is true and default_phase_mode is "code-first"):
1. Read `.planning/prototype/CODE-INVENTORY.md` as primary requirements input (replaces or supplements REQUIREMENTS.md)
2. Treat @gsd-todo tags as task candidates
3. Treat @gsd-context and @gsd-decision tags as architectural context
4. Treat @gsd-constraint tags as hard limits
5. Treat @gsd-risk tags as risk items requiring mitigation

When default_phase_mode is "plan-first" or "hybrid", use standard planning input (REQUIREMENTS.md) supplemented by CODE-INVENTORY.md if it exists.

MUST include: "**ALWAYS use the Write tool to create files** -- never use `Bash(cat << 'EOF')` or heredoc commands for file creation."

Project context block (`<project_context>` XML tag): Same as gsd-planner -- read CLAUDE.md, check .claude/skills/ directory.

Execution flow (`<execution_flow>` XML tag):

Step 0 `check_arc_config` (FIRST step):
Check ARC mode and phase mode by running:
  ARC_ENABLED=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get arc.enabled 2>/dev/null || echo "false")
  PHASE_MODE=$(node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-get default_phase_mode 2>/dev/null || echo "plan-first")

Mode logic:
- If ARC_ENABLED is "false": behave as standard planner, ignore CODE-INVENTORY.md
- If ARC_ENABLED is "true" and PHASE_MODE is "code-first": use CODE-INVENTORY.md as PRIMARY input
- If ARC_ENABLED is "true" and PHASE_MODE is "hybrid": use both REQUIREMENTS.md and CODE-INVENTORY.md
- If ARC_ENABLED is "true" and PHASE_MODE is "plan-first": use REQUIREMENTS.md as primary, CODE-INVENTORY.md as supplementary context

Remaining steps: Replicate the FULL execution flow from gsd-planner.md in prose (Option A delegation). Include ALL of these behaviors:
- Load project state (STATE.md, project history)
- Load codebase context (codebase map if exists)
- Identify phase (from ROADMAP.md)
- Mandatory discovery protocol
- Read project history (prior SUMMARYs)
- Gather phase context (CONTEXT.md, RESEARCH.md)
- Break into tasks (dependency-first decomposition)
- Build dependency graph (needs/creates for each task)
- Assign waves (parallel execution optimization)
- Group into plans (2-3 tasks each, ~50% context target)
- Derive must_haves (goal-backward methodology)
- Write PLAN.md files (with frontmatter: wave, depends_on, files_modified, autonomous, must_haves)
- Validate and commit

MODIFY the gather_phase_context step based on the mode logic from step 0:

When PHASE_MODE is "code-first":
1. Read `.planning/prototype/CODE-INVENTORY.md` as the primary requirements source
2. Run `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" extract-tags --format json` for fresh tag data
3. Each @gsd-todo tag is a task candidate -- derive task name, files, action from the todo text and source file context
4. Use @gsd-context tags to build the phase objective
5. Use @gsd-constraint tags as must_haves.truths
6. Use @gsd-ref(ref:REQ-ID) to populate the `requirements` frontmatter field
7. Still produce standard PLAN.md format with full frontmatter

When PHASE_MODE is "hybrid":
1. Read both REQUIREMENTS.md and CODE-INVENTORY.md
2. Cross-reference: requirements with matching @gsd-ref tags get richer context from annotations
3. @gsd-todo tags not covered by requirements become additional tasks

Constraints block (`<constraints>` XML tag):
1. When arc.enabled is false, behave IDENTICALLY to standard gsd-planner -- no ARC behavior
2. Output format is ALWAYS standard PLAN.md with frontmatter -- the ARC mode changes INPUT, not OUTPUT
3. Do not modify gsd-planner.md or any upstream files
4. CODE-INVENTORY.md is read-only input -- never modify it
5. When code-first mode is active, @gsd-todo tags are the authoritative task list
6. Still apply goal-backward methodology even in code-first mode
7. Use Write tool for all file creation, never heredoc via Bash
  </action>
  <verify>
    <automated>cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node --test tests/agent-frontmatter.test.cjs 2>&1 | grep -E "gsd-arc-planner|pass|fail" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-arc-planner.md exists
    - File contains `name: gsd-arc-planner` in frontmatter
    - File contains `tools: Read, Write, Bash, Glob, Grep, WebFetch, mcp__context7__*` (matches gsd-planner tools)
    - File contains `# hooks:` commented section in frontmatter
    - File contains the phrase `never use \`Bash(cat << 'EOF')\` or heredoc`
    - File contains `config-get arc.enabled` (config gating per D-14)
    - File contains `config-get default_phase_mode` (mode detection per D-15)
    - File contains `|| echo "false"` (graceful fallback)
    - File contains `CODE-INVENTORY.md` (reads annotations as input per AMOD-02)
    - File contains `extract-tags --format json` (supplementary scan)
    - File contains `@gsd-todo` (treats todos as task candidates)
    - File contains `code-first` and `plan-first` and `hybrid` (all three mode paths)
    - File contains `PLAN.md` (produces standard output format)
    - File does NOT contain `skills:` in frontmatter
    - agents/gsd-planner.md is UNMODIFIED (verify with git diff)
    - agent-frontmatter.test.cjs does not report new failures for gsd-arc-planner
  </acceptance_criteria>
  <done>gsd-arc-planner.md passes frontmatter tests, checks arc.enabled and default_phase_mode config, reads CODE-INVENTORY.md in code-first mode, produces standard PLAN.md format, and falls back to standard behavior when disabled</done>
</task>

</tasks>

<verification>
Run: cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node --test tests/agent-frontmatter.test.cjs 2>&1 | tail -5
Both wrapper agents pass all frontmatter tests. No new failures introduced. Upstream agent files unmodified (git diff shows no changes to gsd-executor.md or gsd-planner.md).
</verification>

<success_criteria>
- agents/gsd-arc-executor.md exists with config gating, @gsd-decision addition, @gsd-todo removal
- agents/gsd-arc-planner.md exists with config gating, CODE-INVENTORY.md reading, three mode paths
- Both wrappers are self-contained (Option A delegation) and do not read upstream agents at runtime
- Both wrappers fall back to standard behavior when arc.enabled is false
- agent-frontmatter.test.cjs reports no new failures
- git diff shows zero changes to agents/gsd-executor.md and agents/gsd-planner.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-agents/02-03-SUMMARY.md`
</output>
