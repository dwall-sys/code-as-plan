---
phase: 02-core-agents
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-prototyper.md
  - commands/gsd/prototype.md
autonomous: true
requirements:
  - PROT-01
  - PROT-02
  - PROT-03
  - PROT-04

must_haves:
  truths:
    - "Running /gsd:prototype spawns gsd-prototyper agent with PROJECT.md, REQUIREMENTS.md, ROADMAP.md context"
    - "Prototyper builds scaffold code with @gsd-tags embedded following ARC standard"
    - "Running /gsd:prototype --phases 2 scopes the prototype to phase 2 requirements"
    - "Prototyper writes PROTOTYPE-LOG.md on completion capturing what was built, decisions, and open todos"
    - "Prototype command auto-runs extract-plan on completion to generate CODE-INVENTORY.md"
  artifacts:
    - path: "agents/gsd-prototyper.md"
      provides: "Prototyper agent with ARC-compliant annotation embedding"
      contains: "name: gsd-prototyper"
    - path: "commands/gsd/prototype.md"
      provides: "Slash command spawning gsd-prototyper with auto-chain"
      contains: "name: gsd:prototype"
  key_links:
    - from: "commands/gsd/prototype.md"
      to: "agents/gsd-prototyper.md"
      via: "Task tool spawn with subagent_type gsd-prototyper"
      pattern: "gsd-prototyper"
    - from: "commands/gsd/prototype.md"
      to: "gsd-tools.cjs extract-tags"
      via: "Bash auto-chain after agent completes"
      pattern: "extract-tags --format md --output"
    - from: "agents/gsd-prototyper.md"
      to: "get-shit-done/references/arc-standard.md"
      via: "Read tool at startup"
      pattern: "arc-standard.md"
---

<objective>
Create the gsd-prototyper agent and /gsd:prototype command that enables developers to build annotated prototypes from scratch.

Purpose: The prototyper is the entry point for code-first development -- it generates working scaffold code with @gsd-tags already embedded, then auto-generates CODE-INVENTORY.md for downstream planning.
Output: agents/gsd-prototyper.md, commands/gsd/prototype.md
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@agents/gsd-annotator.md
@commands/gsd/annotate.md
@get-shit-done/references/arc-standard.md

<interfaces>
Pattern templates from Phase 1 that executor must replicate exactly.

From agents/gsd-annotator.md (frontmatter fields): name, description, tools (Read/Write/Edit/Bash/Grep/Glob), permissionMode (acceptEdits), color (green). Wrap in triple-dash YAML frontmatter delimiters.

From agents/gsd-executor.md (commented hooks pattern -- REQUIRED in frontmatter):
  # hooks:
  #   PostToolUse:
  #     - matcher: "Write|Edit"
  #       hooks:
  #         - type: command
  #           command: "npx eslint --fix $FILE 2>/dev/null || true"

From commands/gsd/annotate.md (frontmatter fields): name (gsd:annotate), description, argument-hint, allowed-tools list including Task.

From commands/gsd/annotate.md (auto-chain bash command):
  node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" extract-tags --format md --output .planning/prototype/CODE-INVENTORY.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gsd-prototyper agent</name>
  <files>agents/gsd-prototyper.md</files>
  <read_first>
    - agents/gsd-annotator.md (direct template -- replicate structure exactly)
    - agents/gsd-executor.md (commented hooks pattern -- lines 7-13)
    - get-shit-done/references/arc-standard.md (tag types the prototyper must embed)
    - .planning/REQUIREMENTS.md (requirement IDs prototyper references)
  </read_first>
  <action>
Create `agents/gsd-prototyper.md` with the following exact structure (per D-01, D-02, D-03):

YAML frontmatter (wrapped in triple-dash delimiters, MUST include all of these fields):
  name: gsd-prototyper
  description: Builds working code prototypes with ARC annotations embedded. Spawned by /gsd:prototype command.
  tools: Read, Write, Edit, Bash, Grep, Glob
  permissionMode: acceptEdits
  color: cyan
  # hooks:
  #   PostToolUse:
  #     - matcher: "Write|Edit"
  #       hooks:
  #         - type: command
  #           command: "npx eslint --fix $FILE 2>/dev/null || true"

CRITICAL: The commented `# hooks:` block MUST be inside the frontmatter (between the triple-dash delimiters), not in the body. Do NOT include a `skills:` field.

Role block (XML tag `<role>`):
"You are the GSD prototyper -- you build working code prototypes with @gsd-tags already embedded following the ARC annotation standard. Spawned by `/gsd:prototype` command. You produce runnable scaffold code that demonstrates structure and intent -- not production-ready implementations. Every significant code element gets an appropriate @gsd-tag."
MUST include: "**ALWAYS use the Write tool to create files** -- never use `Bash(cat << 'EOF')` or heredoc commands for file creation."

Project context block (`<project_context>` XML tag, same pattern as gsd-annotator): Read CLAUDE.md, PROJECT.md, REQUIREMENTS.md, ROADMAP.md, and arc-standard.md at startup.

Execution flow (`<execution_flow>` XML tag) with 5 numbered steps:

Step 1 `load_context`: Read PROJECT.md, REQUIREMENTS.md, ROADMAP.md, arc-standard.md. If `$ARGUMENTS` contains `--phases N`, filter REQUIREMENTS.md to only requirements whose phase matches N (check the Traceability table in REQUIREMENTS.md).

Step 2 `plan_prototype`: Based on requirements in scope, plan which files to create. Each file should demonstrate the feature structure. List planned files and their purpose before writing.

Step 3 `build_prototype`: Create each file using the Write tool. Embed @gsd-tags in comments following arc-standard.md rules. Tag types to use: @gsd-context (architectural decisions, module purpose), @gsd-todo (work items that need implementation -- these become tasks for code-planner), @gsd-decision (design choices made during prototyping), @gsd-constraint (hard limits), @gsd-pattern (patterns established), @gsd-ref(ref:REQ-ID) (links to requirement IDs), @gsd-risk (areas of concern), @gsd-api (public interface definitions). Tags must use the comment anchor rule: comment token must be first non-whitespace on the line.

Step 4 `write_prototype_log`: Write `.planning/prototype/PROTOTYPE-LOG.md` (per D-07) with this template structure:
  Heading: # Prototype Log
  Metadata: Date, Phase scope (number or "all"), Requirements addressed (REQ-IDs)
  Section "## What Was Built": table with columns File, Purpose, Tags Added
  Section "## Decisions Made": bullet list of decisions with rationale
  Section "## Open @gsd-todos": checkbox list with todo description, file path, approximate line number
  Section "## Next Steps": text saying to run /gsd:extract-plan then /gsd:iterate

Step 5 `report`: Summarize files created, total tags embedded, tag type breakdown, path to PROTOTYPE-LOG.md.

Constraints block (`<constraints>` XML tag at end of agent prompt):
1. NEVER modify existing code files -- only CREATE new files
2. All @gsd-tags must follow arc-standard.md syntax exactly
3. Comment anchor rule: comment token must be first non-whitespace on the line
4. Prototype code must be runnable -- syntactically valid, imports resolve to real or stubbed modules
5. Do not generate production-ready implementations -- scaffold that shows structure and intent
6. Always write PROTOTYPE-LOG.md on completion
7. If --phases flag provided, ONLY prototype requirements for those phases
8. Use Write tool for all file creation, never heredoc via Bash
  </action>
  <verify>
    <automated>cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node --test tests/agent-frontmatter.test.cjs 2>&1 | grep -E "gsd-prototyper|pass|fail" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-prototyper.md exists
    - File contains `name: gsd-prototyper` in frontmatter
    - File contains `tools: Read, Write, Edit, Bash, Grep, Glob`
    - File contains `permissionMode: acceptEdits`
    - File contains `# hooks:` commented section in frontmatter
    - File contains the phrase `never use \`Bash(cat << 'EOF')\` or heredoc`
    - File contains `arc-standard.md` (reads the ARC standard)
    - File contains `PROJECT.md` (reads project context)
    - File contains `REQUIREMENTS.md` (reads requirements)
    - File contains `ROADMAP.md` (reads roadmap per D-01)
    - File contains `--phases` (supports phase scoping per D-06)
    - File contains `PROTOTYPE-LOG.md` (writes log per D-07)
    - File contains `@gsd-context` and `@gsd-todo` and `@gsd-decision` (tag types)
    - File does NOT contain `skills:` in frontmatter
    - agent-frontmatter.test.cjs does not report new failures for gsd-prototyper
  </acceptance_criteria>
  <done>gsd-prototyper.md passes agent-frontmatter tests (anti-heredoc, commented hooks, no skills:, name matches filename) and contains all required execution steps</done>
</task>

<task type="auto">
  <name>Task 2: Create prototype slash command</name>
  <files>commands/gsd/prototype.md</files>
  <read_first>
    - commands/gsd/annotate.md (direct template -- replicate structure exactly)
    - agents/gsd-prototyper.md (just created -- verify agent name matches)
  </read_first>
  <action>
Create `commands/gsd/prototype.md` following the exact pattern from `commands/gsd/annotate.md` (per D-05, D-06):

YAML frontmatter (wrapped in triple-dash delimiters):
  name: gsd:prototype
  description: Build a working code prototype with embedded @gsd-tags using gsd-prototyper, then auto-run extract-plan
  argument-hint: "[path] [--phases N]"
  allowed-tools:
    - Read
    - Write
    - Edit
    - Bash
    - Task
    - Glob
    - Grep

CRITICAL: `Task` MUST be in `allowed-tools` -- required for agent spawning.

Objective block (`<objective>` XML tag): Spawns the `gsd-prototyper` agent to build working prototype code with `@gsd-tags` embedded following the ARC annotation standard. On completion, automatically runs `extract-plan` to produce `.planning/prototype/CODE-INVENTORY.md`. Arguments: `path` (target directory, defaults to project root), `--phases N` (scope prototype to specific phase numbers from ROADMAP.md, e.g. `--phases 2` or `--phases 2,3`). The prototyper reads PROJECT.md, REQUIREMENTS.md, and ROADMAP.md before building.

Execution context block (`<execution_context>` XML tag):
  @~/.claude/get-shit-done/workflows/prototype.md

Context block (`<context>` XML tag):
  $ARGUMENTS
  @.planning/PROJECT.md
  @.planning/REQUIREMENTS.md
  @.planning/ROADMAP.md

Process block (`<process>` XML tag) with 4 numbered steps mirroring annotate.md:
1. Spawn gsd-prototyper agent via Task tool, passing `$ARGUMENTS` as context. The agent will read arc-standard.md, PROJECT.md, REQUIREMENTS.md, ROADMAP.md, identify phase scope from --phases flag, build prototype files with embedded @gsd-tags, and write PROTOTYPE-LOG.md.
2. Wait for gsd-prototyper to complete and note its summary (files created, tags embedded, breakdown by type).
3. Auto-run extract-plan (per D-04):
   node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" extract-tags --format md --output .planning/prototype/CODE-INVENTORY.md
4. Show the user results: files created (from prototyper summary), total @gsd-tags embedded, path to PROTOTYPE-LOG.md (.planning/prototype/PROTOTYPE-LOG.md), path to CODE-INVENTORY.md (.planning/prototype/CODE-INVENTORY.md).
  </action>
  <verify>
    <automated>cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node --test tests/agent-frontmatter.test.cjs 2>&1 | grep -E "prototype|unknown agent" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - commands/gsd/prototype.md exists
    - File contains `name: gsd:prototype` in frontmatter
    - File contains `Task` in allowed-tools list
    - File contains `argument-hint:` with `--phases`
    - File contains `$ARGUMENTS` in context block
    - File contains `gsd-prototyper` (references correct agent name)
    - File contains `extract-tags --format md --output .planning/prototype/CODE-INVENTORY.md` (auto-chain)
    - File contains `$HOME` not `~` in bash commands
    - File contains `PROTOTYPE-LOG.md`
    - File contains `PROJECT.md` and `REQUIREMENTS.md` and `ROADMAP.md` in context
    - agent-frontmatter.test.cjs "named agent spawns use correct agent names" does not fail for prototype.md
  </acceptance_criteria>
  <done>prototype.md spawns gsd-prototyper, supports --phases flag, auto-chains to extract-plan, and passes agent spawn validation test</done>
</task>

</tasks>

<verification>
Run: cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node --test tests/agent-frontmatter.test.cjs 2>&1 | tail -5
All gsd-prototyper tests pass. No new test failures introduced (pre-existing gsd-annotator failures are acceptable).
</verification>

<success_criteria>
- agents/gsd-prototyper.md exists with valid frontmatter and complete execution flow
- commands/gsd/prototype.md exists with Task in allowed-tools and auto-chain to extract-plan
- agent-frontmatter.test.cjs reports no new failures for gsd-prototyper or prototype.md
- PROTOTYPE-LOG.md template is embedded in the prototyper agent prompt
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-agents/02-01-SUMMARY.md`
</output>
