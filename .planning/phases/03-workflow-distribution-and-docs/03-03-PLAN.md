---
phase: 03-workflow-distribution-and-docs
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - get-shit-done/workflows/help.md
  - README.md
autonomous: false
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "help command output lists all 6 new code-first commands with one-line descriptions"
    - "README.md explains installation via npx gsd-code-first@latest"
    - "README.md documents the code-first workflow: prototype -> iterate pattern"
    - "README.md explains ARC tags and mode switching"
    - "README.md references arc-standard.md for detailed tag syntax rather than duplicating it"
    - "New agent and command files are picked up by installer wholesale directory copy without code changes"
    - "package.json name is gsd-code-first with bin entry get-shit-done-cc"
  artifacts:
    - path: "get-shit-done/workflows/help.md"
      provides: "Updated help reference with Code-First Commands section"
      contains: "Code-First Commands"
    - path: "README.md"
      provides: "Fork documentation with installation, workflow, and user guide"
      contains: "npx gsd-code-first@latest"
  key_links:
    - from: "commands/gsd/help.md"
      to: "get-shit-done/workflows/help.md"
      via: "execution_context reference"
      pattern: "help\\.md"
    - from: "README.md"
      to: "get-shit-done/references/arc-standard.md"
      via: "documentation cross-reference"
      pattern: "arc-standard\\.md"
---

<objective>
Verify distribution readiness (installer, package.json, GSD_CF_ namespace) and create all documentation: updated help command reference with new commands, README.md with installation/workflow/user guide.

Purpose: Without documentation, users cannot discover or learn the code-first workflow. Without distribution verification, `npx gsd-code-first@latest` may not work. This plan ships the user-facing layer.
Output: Updated help.md, updated README.md, verified distribution readiness.
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-distribution-and-docs/03-CONTEXT.md
@.planning/phases/03-workflow-distribution-and-docs/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify distribution and update help workflow</name>
  <read_first>
    - package.json (verify name, bin entry, version, files array)
    - bin/install.js (lines 4180-4256 -- confirm wholesale directory copy of agents/ and commands/gsd/)
    - get-shit-done/workflows/help.md (current help reference -- find where to add Code-First Commands section)
    - commands/gsd/help.md (confirm it references get-shit-done/workflows/help.md)
  </read_first>
  <files>get-shit-done/workflows/help.md</files>
  <action>
**Distribution verification (DIST-01, DIST-02, DIST-03):** Verify these conditions are already met (no code changes expected):
1. `package.json` has `"name": "gsd-code-first"` and `"bin": { "get-shit-done-cc": "bin/install.js" }` -- per D-16
2. `bin/install.js` uses `fs.readdirSync` on `agents/` and `commands/gsd/` to copy ALL .md files wholesale -- per Research Pattern 4. No per-file registration is needed for DIST-01.
3. New agent files (gsd-prototyper.md, gsd-code-planner.md, gsd-arc-executor.md, gsd-arc-planner.md) exist in `agents/` directory
4. New command files (prototype.md, annotate.md, extract-plan.md, iterate.md, set-mode.md, deep-plan.md) exist in `commands/gsd/` directory
5. Agent/command descriptions contain "Code-First" or "gsd-code-first" for namespace identification -- per D-15, Research Pattern 5

If any verification fails, fix it. If all pass, document the verification results.

**Help workflow update (DOCS-01):** Edit `get-shit-done/workflows/help.md` to add a "Code-First Commands" section. Per D-17, D-18. Add this section AFTER the existing command sections (find the last command section and insert after it). The section content must be exactly:

## Code-First Commands

These commands are added by the `gsd-code-first` fork for code-first development workflows.

**`/gsd:prototype`**
Build a working code prototype with embedded @gsd-tags, then auto-extract CODE-INVENTORY.md.
Usage: `/gsd:prototype [path] [--phases N]`

**`/gsd:annotate`**
Retroactively annotate existing code with @gsd-tags, then auto-extract CODE-INVENTORY.md.
Usage: `/gsd:annotate [path] [--glob PATTERN]`

**`/gsd:extract-plan`**
Scan codebase for @gsd-tags and produce .planning/prototype/CODE-INVENTORY.md.
Usage: `/gsd:extract-plan [--phase N] [--type TAG_TYPE]`

**`/gsd:iterate`**
Run the full code-first loop: extract tags, generate plan, approve, execute.
Usage: `/gsd:iterate [--non-interactive] [--verify] [--annotate]`

**`/gsd:set-mode`**
Configure the per-phase workflow mode (code-first, plan-first, hybrid).
Usage: `/gsd:set-mode <mode> [--phase N]`

**`/gsd:deep-plan`**
Chain discuss-phase then plan-phase for phases needing upfront reasoning.
Usage: `/gsd:deep-plan <phase-number> [flags]`
  </action>
  <verify>
    <automated>grep -q "Code-First Commands" get-shit-done/workflows/help.md && grep -q "gsd:iterate" get-shit-done/workflows/help.md && grep -q "gsd:set-mode" get-shit-done/workflows/help.md && grep -q "gsd:deep-plan" get-shit-done/workflows/help.md && grep -q "gsd:prototype" get-shit-done/workflows/help.md && grep -q "gsd:annotate" get-shit-done/workflows/help.md && grep -q "gsd:extract-plan" get-shit-done/workflows/help.md && echo "PASS: all 6 commands in help" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - get-shit-done/workflows/help.md contains a section titled "## Code-First Commands"
    - Help section lists all 6 commands: /gsd:prototype, /gsd:annotate, /gsd:extract-plan, /gsd:iterate, /gsd:set-mode, /gsd:deep-plan
    - Each command has a one-line description (per D-18)
    - Each command has a Usage line showing argument format
    - package.json `name` field is `gsd-code-first` (verified, not changed)
    - package.json `bin.get-shit-done-cc` is `bin/install.js` (verified, not changed)
    - All new agent .md files exist in agents/ directory
    - All new command .md files exist in commands/gsd/ directory
  </acceptance_criteria>
  <done>Help workflow lists all code-first commands; distribution verification confirms installer will copy all new files without code changes</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md code-first documentation and user guide</name>
  <read_first>
    - README.md (current content -- understand existing structure to prepend fork section, per Research Pitfall 4)
    - get-shit-done/references/arc-standard.md (tag reference -- must cross-reference not duplicate, per D-21)
    - commands/gsd/iterate.md (iterate workflow details for documentation accuracy)
    - commands/gsd/set-mode.md (mode switching details for documentation accuracy)
  </read_first>
  <files>README.md</files>
  <action>
**README.md update (DOCS-02, DOCS-03):** Per D-19, D-20, D-21 and Research Pitfall 4 (upstream drift). Strategy: prepend a fork-specific section at the top, separated by a clear divider from the existing upstream GSD content.

Insert at the TOP of README.md (before existing content), these sections in order:

**1. Header:**
  # GSD Code-First
  > Code is the plan -- build first, extract structured planning from annotated code.
  A fork of GSD (Get Shit Done) that implements code-first development with Claude Code. Instead of running discuss -> plan -> execute for every phase, developers build a prototype directly, annotate the code with structured @gsd-tags (the "ARC" system), and use those annotations as planning input for further iterations.

**2. Installation section:**
  ## Installation
  Code block with: npx gsd-code-first@latest
  Explain: installs GSD Code-First fork into Claude Code environment. All original GSD commands continue working -- code-first commands are additive.

**3. Quick Start section:**
  ## Quick Start: Code-First Workflow
  Step 1: /gsd:prototype --phases 1 (build prototype with embedded annotations)
  Step 2: /gsd:iterate (extract -> plan -> approve -> execute)
  Step 3: Repeat -- iterate as many times as needed
  The Full Loop diagram: /gsd:prototype -> code with @gsd-tags -> /gsd:iterate -> approve plan -> execute -> repeat

**4. ARC Annotations section (user guide per DOCS-03):**
  ## ARC Annotations
  Brief explanation of ARC tags as structured comments that planning agents read. Show one example block with @gsd-todo, @gsd-decision, @gsd-constraint in JavaScript comment format.
  List tag types: @gsd-context, @gsd-decision, @gsd-todo, @gsd-constraint, @gsd-pattern, @gsd-ref, @gsd-risk, @gsd-api
  Cross-reference: "For full tag syntax, metadata format, and per-language examples, see arc-standard.md" with a relative link to get-shit-done/references/arc-standard.md
  Do NOT duplicate the full tag syntax from arc-standard.md (per D-21).

**5. Workflow Modes section (per DOCS-03):**
  ## Workflow Modes
  Table with three modes:
    code-first: prototype -> iterate. When: You know what to build, want to move fast.
    plan-first: discuss -> plan -> execute. When: Complex domain, need upfront reasoning.
    hybrid: Mix per preference. When: Some phases code-first, others plan-first.
  Show commands: /gsd:set-mode code-first, /gsd:set-mode plan-first --phase 3, /gsd:deep-plan 3

**6. Code-First Commands table:**
  ## Code-First Commands
  Table with columns Command and Description listing all 6 commands:
    /gsd:prototype - Build working prototype with @gsd-tags
    /gsd:annotate - Retroactively add @gsd-tags to existing code
    /gsd:extract-plan - Scan code for @gsd-tags, produce CODE-INVENTORY.md
    /gsd:iterate - Full code-first loop: extract -> plan -> approve -> execute
    /gsd:set-mode - Configure workflow mode (code-first, plan-first, hybrid)
    /gsd:deep-plan - Chain discuss + plan for upfront reasoning phases
  Note: "All original GSD commands continue working unchanged."

**7. Divider before upstream content:**
  A horizontal rule, then italic text: "The sections below are from the original GSD framework. All original commands and workflows remain fully functional."
  Another horizontal rule, then the existing README.md content follows unchanged.
  </action>
  <verify>
    <automated>grep -q "npx gsd-code-first@latest" README.md && grep -q "ARC Annotations" README.md && grep -q "Workflow Modes" README.md && grep -q "Code-First Commands" README.md && grep -q "gsd:iterate" README.md && grep -q "arc-standard.md" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - README.md begins with "# GSD Code-First" header (fork branding)
    - README.md contains "## Installation" section with `npx gsd-code-first@latest`
    - README.md contains "## Quick Start: Code-First Workflow" section showing prototype -> iterate
    - README.md contains "## ARC Annotations" section with example tags and link to arc-standard.md
    - README.md does NOT contain full tag syntax tables duplicated from arc-standard.md (per D-21)
    - README.md contains "## Workflow Modes" section with table of code-first, plan-first, hybrid
    - README.md contains "## Code-First Commands" table listing all 6 new commands
    - README.md contains a horizontal rule divider before the original upstream GSD content
    - Original upstream README content is preserved below the divider (per Research Pitfall 4)
  </acceptance_criteria>
  <done>README.md documents installation, code-first workflow, ARC tags, mode switching, and all commands while preserving upstream content for merge compatibility</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 3 delivery</name>
  <files>none (verification only)</files>
  <action>
Present the complete Phase 3 delivery for human verification. All deliverables:
- iterate command (commands/gsd/iterate.md)
- set-mode command (commands/gsd/set-mode.md)
- deep-plan command (commands/gsd/deep-plan.md)
- Distribution verification (package.json, installer, agent/command files)
- Help update (get-shit-done/workflows/help.md)
- README documentation (README.md)

Ask the user to verify:
1. Run `/gsd:help` and confirm all 6 code-first commands appear with descriptions
2. Open README.md and confirm it explains: installation, prototype -> iterate workflow, ARC tags, mode switching
3. Run `node get-shit-done/bin/gsd-tools.cjs set-mode code-first` and confirm it succeeds
4. Confirm `commands/gsd/iterate.md` exists and contains the approval gate logic
5. Confirm `commands/gsd/deep-plan.md` exists and references discuss-phase + plan-phase

Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: requires human verification"</automated>
  </verify>
  <done>Human has reviewed and approved all Phase 3 deliverables</done>
</task>

</tasks>

<verification>
- `get-shit-done/workflows/help.md` contains "Code-First Commands" section with all 6 commands
- `README.md` contains installation, workflow, ARC tags, and mode switching documentation
- `README.md` references arc-standard.md (per D-21) without duplicating tag syntax
- Distribution: package.json correct, installer copies files wholesale, new files in correct directories
- All Phase 3 requirements (DIST-01 through DOCS-03) are addressed
</verification>

<success_criteria>
- Help command shows all 6 code-first commands
- README.md is a complete user entry point for gsd-code-first
- Distribution verification confirms npx gsd-code-first@latest will install correctly
- Human has verified the complete delivery
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-distribution-and-docs/03-03-SUMMARY.md`
</output>
