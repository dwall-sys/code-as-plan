---
phase: 01-annotation-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - agents/gsd-annotator.md
  - commands/gsd/annotate.md
autonomous: true
requirements:
  - ANNOT-01
  - ANNOT-02
must_haves:
  truths:
    - "gsd-annotator agent exists at agents/gsd-annotator.md with YAML frontmatter name gsd-annotator"
    - "Agent prompt instructs reading PROJECT.md, REQUIREMENTS.md, and arc-standard.md before annotating"
    - "Agent produces @gsd-tags anchored to comment tokens, matching the ARC standard from Plan 01"
    - "annotate command exists at commands/gsd/annotate.md and auto-runs extract-plan on completion"
    - "Neither file modifies any existing agent or command file"
  artifacts:
    - path: "agents/gsd-annotator.md"
      provides: "Retroactive annotation agent prompt"
      contains: "name: gsd-annotator"
    - path: "commands/gsd/annotate.md"
      provides: "annotate slash command that spawns gsd-annotator then runs extract-plan"
      contains: "name: gsd:annotate"
  key_links:
    - from: "commands/gsd/annotate.md"
      to: "agents/gsd-annotator.md"
      via: "Task tool spawns gsd-annotator agent"
      pattern: "gsd-annotator"
    - from: "commands/gsd/annotate.md"
      to: "commands/gsd/extract-plan.md"
      via: "Auto-runs extract-plan on annotator completion"
      pattern: "extract-plan"
---

<objective>
Create the `gsd-annotator` agent and the `annotate` slash command that spawns it.

Purpose: gsd-annotator is the retroactive annotation agent — it reads existing code and adds @gsd-tags following the ARC standard. The annotate command spawns it and auto-runs extract-plan on completion (D-12). Both are new files. No existing files are modified.

Output: `agents/gsd-annotator.md` (agent prompt) and `commands/gsd/annotate.md` (slash command).
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/phases/01-annotation-foundation/01-CONTEXT.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/phases/01-annotation-foundation/01-RESEARCH.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/phases/01-annotation-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Locked decisions for the annotator. Executor must implement ALL of these exactly. -->

D-11: gsd-annotator operates at directory scope with file glob filtering
D-12: annotate command spawns gsd-annotator then auto-runs extract-plan on completion
D-13: Annotator reads existing code + PROJECT.md + REQUIREMENTS.md to determine appropriate annotations

Agent frontmatter fields (RESEARCH.md Pattern 4 — replicate this format in agents/gsd-annotator.md):
  name: gsd-annotator
  description: Retroactively annotates existing code with @gsd-tags following the ARC standard. Spawned by /gsd:annotate command.
  tools: Read, Write, Edit, Bash, Grep, Glob
  permissionMode: acceptEdits
  color: green

From agents/gsd-executor.md (the template agent — executor MUST read this file):
  Body organized as: role block, project_context block, numbered execution_flow steps
  References workflow files via @-paths

ARC standard reference (written in Plan 01):
  get-shit-done/references/arc-standard.md

CRITICAL: Do NOT modify agents/gsd-executor.md or any other existing agent file. Create only new files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gsd-annotator.md agent</name>
  <files>agents/gsd-annotator.md</files>
  <read_first>
    - agents/gsd-executor.md (MANDATORY — read the ENTIRE file; replicate the YAML frontmatter format exactly, role block structure, project_context block, and execution_flow section pattern)
    - agents/gsd-planner.md (read for a second agent example — compare structure and body sections)
    - get-shit-done/references/arc-standard.md (read for exact tag types, comment anchor rules, and metadata syntax the annotator must produce)
    - .planning/phases/01-annotation-foundation/01-CONTEXT.md (D-11, D-12, D-13 — annotator scope and behavior)
  </read_first>
  <action>
Create `agents/gsd-annotator.md` as a NEW file. Do NOT modify any existing file in agents/.

The file must begin with YAML frontmatter between triple-dash delimiters with these exact fields:
  name: gsd-annotator
  description: Retroactively annotates existing code with @gsd-tags following the ARC standard. Spawned by /gsd:annotate command.
  tools: Read, Write, Edit, Bash, Grep, Glob
  permissionMode: acceptEdits
  color: green

After the frontmatter, the body must contain these sections — follow the exact section names and structure from gsd-executor.md:

ROLE BLOCK: Explain the agent's purpose as "the GSD annotator — reads existing source code and adds @gsd-tags in comment form following the ARC annotation standard." State: "Spawned by /gsd:annotate command." State: "You operate at directory scope — annotate all files matching the glob pattern provided." Include a CRITICAL note: "Do not change any existing code logic, function signatures, or existing comments. Only ADD @gsd-tag lines in appropriate positions."

PROJECT CONTEXT BLOCK: Same pattern as gsd-executor.md. Instructs agent to (1) read ./CLAUDE.md if it exists, (2) read PROJECT.md for project goals, (3) read REQUIREMENTS.md for requirement IDs to reference in tags, (4) read `get-shit-done/references/arc-standard.md` for the ARC annotation standard.

EXECUTION FLOW BLOCK — Four numbered steps:

Step 1, Load context: Read PROJECT.md and REQUIREMENTS.md. Read get-shit-done/references/arc-standard.md. Note all requirement IDs (use in @gsd-ref metadata and phase metadata).

Step 2, Identify files to annotate: Use Glob to find files matching the provided pattern. Default glob: files with extensions js, ts, py, go, rs, sql, or sh in the target directory. Exclude: node_modules/, .git/, .planning/, dist/, build/. List files found before annotating.

Step 3, Annotate each file: For each file — Read the file, identify entry points, key functions, design decisions visible in code, constraints, todos, patterns. For each significant code block: add one or more @gsd-tags as comment lines IMMEDIATELY before the relevant line/function/class. Follow comment syntax for the file's language (// for JS/TS/Go/Rust, # for Python/Shell, -- for SQL). Use phase: metadata when code clearly belongs to a roadmap phase. Use ref: metadata to reference a REQUIREMENTS.md ID when code implements a requirement. Write the annotated file using Edit or Write tool.

Step 4, Report: Print a summary of files annotated, total tags added, and tag type breakdown. Note that the annotate command will auto-run extract-plan after this agent completes.

CONSTRAINTS BLOCK: List hard rules: never change function signatures, variable names, or logic; never remove existing comments; tags MUST be on their own comment line anchored to the language's comment token; do not add tags to generated files, vendored code, or test fixtures; use only the 8 tag types from arc-standard.md (context, decision, todo, constraint, pattern, ref, risk, api).
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const c = fs.readFileSync('agents/gsd-annotator.md', 'utf-8');
const checks = [
  'name: gsd-annotator',
  'permissionMode: acceptEdits',
  'tools: Read, Write, Edit, Bash, Grep, Glob',
  'color: green',
  'arc-standard.md',
  'REQUIREMENTS.md',
  'PROJECT.md',
  '@gsd-',
  'comment'
];
const missing = checks.filter(k => !c.includes(k));
if (missing.length) { console.error('MISSING:', missing); process.exit(1); }
console.log('OK — gsd-annotator.md valid');
"</automated>
  </verify>
  <acceptance_criteria>
    - `agents/gsd-annotator.md` exists
    - File begins with YAML frontmatter (triple-dash delimiters)
    - File contains `name: gsd-annotator` in the frontmatter
    - File contains `permissionMode: acceptEdits`
    - File contains `color: green`
    - File contains `tools: Read, Write, Edit, Bash, Grep, Glob`
    - File contains `arc-standard.md` (reference to the ARC spec)
    - File contains `REQUIREMENTS.md` (per D-13)
    - File contains `PROJECT.md` (per D-13)
    - File does NOT contain any require() or Node.js code (it is a Markdown agent prompt)
    - No existing agent file was modified
  </acceptance_criteria>
  <done>agents/gsd-annotator.md created with correct YAML frontmatter, role block, execution steps referencing arc-standard.md, PROJECT.md, REQUIREMENTS.md, and constraints block</done>
</task>

<task type="auto">
  <name>Task 2: Create annotate slash command</name>
  <files>commands/gsd/annotate.md</files>
  <read_first>
    - commands/gsd/quick.md (MANDATORY — read the ENTIRE file; annotate.md follows this exact frontmatter structure and body section structure)
    - commands/gsd/extract-plan.md (the command annotate auto-chains to — note how it is invoked via Bash)
    - agents/gsd-annotator.md (the agent this command spawns — confirm the agent name exactly)
    - .planning/phases/01-annotation-foundation/01-CONTEXT.md (D-12 — auto-run extract-plan on completion)
  </read_first>
  <action>
Create `commands/gsd/annotate.md` as a NEW file following the quick.md command file pattern. Do NOT modify any existing command file.

The file must begin with YAML frontmatter between triple-dash delimiters with these exact fields:
  name: gsd:annotate
  description: Retroactively annotate existing code with @gsd-tags using gsd-annotator, then auto-run extract-plan
  argument-hint: "[path] [--glob PATTERN]"
  allowed-tools: list containing Read, Write, Edit, Bash, Task, Glob, Grep

After the frontmatter, the body must follow quick.md's section structure:

OBJECTIVE SECTION: Spawns gsd-annotator to read existing source code and add @gsd-tags following the ARC standard. On completion, automatically runs extract-plan to produce .planning/prototype/CODE-INVENTORY.md. Arguments: path (directory to annotate, defaults to project root), --glob (file pattern override).

EXECUTION CONTEXT SECTION: Contains the @-reference: @~/.claude/get-shit-done/workflows/annotate.md (for future use).

CONTEXT SECTION: Passes $ARGUMENTS through. Also references: @.planning/PROJECT.md and @.planning/REQUIREMENTS.md

PROCESS SECTION: Four numbered steps:
  1. Spawn gsd-annotator agent via the Task tool, passing $ARGUMENTS as context. The agent will read arc-standard.md, PROJECT.md, and REQUIREMENTS.md before annotating.
  2. Wait for gsd-annotator to complete and note its summary (files annotated, tags added).
  3. Auto-run extract-plan: execute node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" extract-tags --format md --output .planning/prototype/CODE-INVENTORY.md to produce the updated CODE-INVENTORY.md.
  4. Show the user: files annotated, tags added, and path to CODE-INVENTORY.md.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const c = fs.readFileSync('commands/gsd/annotate.md', 'utf-8');
const checks = ['name: gsd:annotate', 'allowed-tools:', 'Task', 'gsd-annotator', 'extract-tags', 'CODE-INVENTORY.md', '.planning/prototype'];
const missing = checks.filter(k => !c.includes(k));
if (missing.length) { console.error('MISSING:', missing); process.exit(1); }
console.log('OK — annotate.md valid');
"</automated>
  </verify>
  <acceptance_criteria>
    - `commands/gsd/annotate.md` exists
    - File contains `name: gsd:annotate` in its YAML frontmatter
    - File contains `Task` in allowed-tools (to spawn gsd-annotator)
    - File contains `gsd-annotator` (references the agent by name)
    - File contains `extract-tags` or `extract-plan` (the auto-chain per D-12)
    - File contains `.planning/prototype` (CODE-INVENTORY.md output path)
    - No existing command file was modified
  </acceptance_criteria>
  <done>commands/gsd/annotate.md created; references gsd-annotator agent; auto-chains to extract-plan; output path is .planning/prototype/CODE-INVENTORY.md</done>
</task>

</tasks>

<verification>
node -e "
['agents/gsd-annotator.md', 'commands/gsd/annotate.md'].forEach(f => {
  const c = require('fs').readFileSync(f, 'utf-8');
  const name = f.includes('gsd-annotator') ? 'gsd-annotator' : 'gsd:annotate';
  if (!c.includes('name: ' + name)) { console.error(f + ' missing name field'); process.exit(1); }
});
console.log('Both files exist with correct frontmatter names');
"
</verification>

<success_criteria>
`agents/gsd-annotator.md` exists with YAML frontmatter (name gsd-annotator, tools, permissionMode acceptEdits, color green) and instructs reading arc-standard.md, PROJECT.md, REQUIREMENTS.md. `commands/gsd/annotate.md` exists, spawns gsd-annotator via Task tool, and auto-runs extract-plan on completion. No existing files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-annotation-foundation/01-05-SUMMARY.md`
</output>
