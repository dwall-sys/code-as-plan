---
phase: 01-annotation-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/config.cjs
autonomous: true
requirements:
  - MODE-02
must_haves:
  truths:
    - "config.cjs VALID_CONFIG_KEYS Set contains 'arc.enabled', 'arc.tag_prefix', 'arc.comment_anchors', 'phase_modes.default'"
    - "buildNewProjectConfig() hardcoded defaults include arc and phase_modes sections"
    - "buildNewProjectConfig() return statement deep-merges arc and phase_modes"
    - "All existing config.cjs tests still pass"
  artifacts:
    - path: "get-shit-done/bin/lib/config.cjs"
      provides: "Extended config schema with arc and phase_modes namespaces"
      contains: "arc.enabled"
  key_links:
    - from: "get-shit-done/bin/lib/config.cjs"
      to: ".planning/config.json"
      via: "buildNewProjectConfig() writes arc and phase_modes to new project configs"
      pattern: "arc.*enabled"
---

<objective>
Extend `get-shit-done/bin/lib/config.cjs` with the `arc` and `phase_modes` config namespaces required by MODE-02.

Purpose: The config schema must support `arc` settings (enabled, tag_prefix, comment_anchors) and `phase_modes` (per-phase workflow mode map + default) before Phase 2 agents try to read them. The changes are strictly additive — no existing keys, defaults, or merge logic are touched.

Output: Three additive changes to config.cjs: new keys in VALID_CONFIG_KEYS, new defaults in buildNewProjectConfig, new deep-merge entries in the return statement.
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/phases/01-annotation-foundation/01-CONTEXT.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/phases/01-annotation-foundation/01-RESEARCH.md

<interfaces>
<!-- Locked decisions for config extension. Executor must implement ALL of these exactly. -->

D-14: New top-level keys `arc` and `phase_modes` added to .planning/config.json schema
D-15: `arc` config section: { enabled: boolean, tag_prefix: "@gsd-", comment_anchors: ["//", "#", "/*", "--"] }
D-16: `phase_modes` config section: maps phase numbers to mode strings ("code-first", "plan-first", "hybrid")
D-17: Config validation extended in get-shit-done/bin/lib/config.cjs

From RESEARCH.md (Open Question 2 resolution): Add `default_phase_mode: "plan-first"` as a top-level config key.

Config extension pattern from RESEARCH.md (Pattern 5 — use EXACTLY this):

Step 1 — Add to VALID_CONFIG_KEYS Set:
  'arc.enabled', 'arc.tag_prefix', 'arc.comment_anchors',
  'phase_modes.default', 'default_phase_mode',

Step 2 — Add to hardcoded defaults in buildNewProjectConfig():
  arc: {
    enabled: true,
    tag_prefix: '@gsd-',
    comment_anchors: ['//', '#', '/*', '--'],
  },
  phase_modes: {},
  default_phase_mode: 'plan-first',

Step 3 — Add to deep-merge return at bottom of buildNewProjectConfig():
  arc: {
    ...hardcoded.arc,
    ...(userDefaults.arc || {}),
    ...(choices.arc || {}),
  },
  phase_modes: {
    ...hardcoded.phase_modes,
    ...(userDefaults.phase_modes || {}),
    ...(choices.phase_modes || {}),
  },
  default_phase_mode: choices.default_phase_mode || userDefaults.default_phase_mode || hardcoded.default_phase_mode,

CRITICAL upstream policy: These are ADDITIVE changes only. Do NOT delete, move, or modify any existing line in config.cjs. Only insert new content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend config.cjs with arc and phase_modes schema</name>
  <files>get-shit-done/bin/lib/config.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/config.cjs (MANDATORY — read the ENTIRE file before touching anything; locate: VALID_CONFIG_KEYS Set definition, hardcoded object in buildNewProjectConfig, the return statement with deep-merge spreads)
    - .planning/phases/01-annotation-foundation/01-RESEARCH.md (Pattern 5: Config Schema Extension — read the exact code to insert)
    - .planning/phases/01-annotation-foundation/01-CONTEXT.md (D-14 through D-17 — locked decisions)
    - tests/config.test.cjs (read to understand what existing tests cover — must not break them)
  </read_first>
  <action>
Make three ADDITIVE-ONLY changes to `get-shit-done/bin/lib/config.cjs`. Do not remove, move, or modify any existing lines.

**Change 1: Add to VALID_CONFIG_KEYS Set**
Locate the `const VALID_CONFIG_KEYS = new Set([...])` definition. Add these keys inside the Set, after the last existing key (before the closing `]);`):

```javascript
  'arc.enabled', 'arc.tag_prefix', 'arc.comment_anchors',
  'phase_modes.default', 'default_phase_mode',
```

**Change 2: Add to hardcoded defaults object**
Locate the `const hardcoded = { ... }` object inside `buildNewProjectConfig()`. Add these properties after the last existing property in the object (before the closing `};`):

```javascript
  arc: {
    enabled: true,
    tag_prefix: '@gsd-',
    comment_anchors: ['//', '#', '/*', '--'],
  },
  phase_modes: {},
  default_phase_mode: 'plan-first',
```

**Change 3: Add to the return statement's deep-merge**
Locate the `return { ...hardcoded, ...userDefaults, ...choices, git: { ... }, workflow: { ... }, ... }` statement at the end of `buildNewProjectConfig()`. Add these entries after the last existing spread property (before the closing `};`):

```javascript
  arc: {
    ...hardcoded.arc,
    ...(userDefaults.arc || {}),
    ...(choices.arc || {}),
  },
  phase_modes: {
    ...hardcoded.phase_modes,
    ...(userDefaults.phase_modes || {}),
    ...(choices.phase_modes || {}),
  },
  default_phase_mode: choices.default_phase_mode || userDefaults.default_phase_mode || hardcoded.default_phase_mode,
```

After all changes, run the existing config test to verify nothing is broken: `node --test tests/config.test.cjs`
  </action>
  <verify>
    <automated>node -e "
const config = require('./get-shit-done/bin/lib/config.cjs');
const result = config.buildNewProjectConfig({});
if (!result.arc) { console.error('FAIL: arc missing'); process.exit(1); }
if (result.arc.tag_prefix !== '@gsd-') { console.error('FAIL: arc.tag_prefix wrong'); process.exit(1); }
if (!Array.isArray(result.arc.comment_anchors)) { console.error('FAIL: comment_anchors not array'); process.exit(1); }
if (result.default_phase_mode !== 'plan-first') { console.error('FAIL: default_phase_mode wrong'); process.exit(1); }
if (typeof result.phase_modes !== 'object') { console.error('FAIL: phase_modes not object'); process.exit(1); }
console.log('OK — arc and phase_modes in config');
" && node --test tests/config.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `get-shit-done/bin/lib/config.cjs` contains `'arc.enabled'` (grep confirms key in VALID_CONFIG_KEYS)
    - `get-shit-done/bin/lib/config.cjs` contains `tag_prefix: '@gsd-'` (grep confirms hardcoded default)
    - `get-shit-done/bin/lib/config.cjs` contains `default_phase_mode: 'plan-first'` (grep confirms default value)
    - `get-shit-done/bin/lib/config.cjs` contains `...hardcoded.arc` in the return statement (grep confirms deep-merge)
    - `node -e "const c=require('./get-shit-done/bin/lib/config.cjs'); const r=c.buildNewProjectConfig({}); if(!r.arc||r.arc.tag_prefix!=='@gsd-')process.exit(1);"` exits 0
    - `node --test tests/config.test.cjs` exits 0 (all existing config tests pass — no regressions)
  </acceptance_criteria>
  <done>config.cjs extended with arc, phase_modes, and default_phase_mode; buildNewProjectConfig() returns correct defaults; all existing config tests still pass</done>
</task>

</tasks>

<verification>
node -e "const c=require('./get-shit-done/bin/lib/config.cjs'); const r=c.buildNewProjectConfig({}); console.log('arc:', JSON.stringify(r.arc)); console.log('phase_modes:', JSON.stringify(r.phase_modes)); console.log('default_phase_mode:', r.default_phase_mode);"
node --test tests/config.test.cjs
</verification>

<success_criteria>
`buildNewProjectConfig({})` returns an object with `arc: { enabled: true, tag_prefix: '@gsd-', comment_anchors: ['//','#','/*','--'] }`, `phase_modes: {}`, and `default_phase_mode: 'plan-first'`. All existing config tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-annotation-foundation/01-04-SUMMARY.md`
</output>
