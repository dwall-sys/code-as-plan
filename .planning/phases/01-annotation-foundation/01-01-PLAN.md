---
phase: 01-annotation-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/arc-standard.md
autonomous: true
requirements:
  - ARC-01
  - ARC-02
must_haves:
  truths:
    - "Developer can find arc-standard.md at get-shit-done/references/arc-standard.md"
    - "Document states version 1.0 and stability guarantee (tag names are frozen)"
    - "All eight tag types are defined with purpose and example"
    - "Document shows the comment-anchor rule (tags only valid on comment lines)"
    - "Per-language examples exist for JavaScript, Python, Go, Rust, SQL, and Shell"
    - "Metadata syntax (parenthesized key:value) is documented with optional and multi-key forms"
  artifacts:
    - path: "get-shit-done/references/arc-standard.md"
      provides: "ARC annotation standard v1.0 — the spec all scanner and agent code implements"
      contains: "## Tag Syntax"
  key_links:
    - from: "get-shit-done/references/arc-standard.md"
      to: "get-shit-done/bin/lib/arc-scanner.cjs (Plan 02)"
      via: "Scanner regex implements the exact comment-anchor rules stated in this document"
      pattern: "Comment Anchor Rule"
---

<objective>
Create the ARC (Annotated Reasoning in Code) annotation standard reference document at `get-shit-done/references/arc-standard.md`.

Purpose: This document IS the spec. Every downstream component — the scanner regex, the annotator agent prompt, the CODE-INVENTORY.md format — must implement what this document defines. It must be stable from day one because changes after annotation sessions force re-annotation.

Output: `get-shit-done/references/arc-standard.md` — a complete, versioned, human-readable specification of @gsd-tag syntax, the eight tag types, comment-anchor rules, optional metadata format, and per-language examples.
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/PROJECT.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/ROADMAP.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/phases/01-annotation-foundation/01-CONTEXT.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.planning/phases/01-annotation-foundation/01-RESEARCH.md

<interfaces>
<!-- Locked decisions that define the content of arc-standard.md. Executor must implement ALL of these. -->

D-01: Tags use single-line structured format: `@gsd-todo(phase:2) Description text` with parenthesized metadata
D-02: Tags are anchored to comment tokens only (`//`, `#`, `/*`, `--`, `"""`, `'''`) — regex must NOT match @gsd- in strings, URLs, or template literals
D-03: Eight tag types: @gsd-context, @gsd-decision, @gsd-todo, @gsd-constraint, @gsd-pattern, @gsd-ref, @gsd-risk, @gsd-api
D-04: Metadata in parentheses is optional — `@gsd-todo Fix this` is valid, `@gsd-todo(phase:2, priority:high) Fix this` adds structured metadata

Tag definitions (content to use in the document):
  @gsd-context    — Explain WHY this code exists, what problem it solves, architectural context
  @gsd-decision   — Record a design/implementation choice and its rationale
  @gsd-todo       — Planned future work, optionally scoped to a phase
  @gsd-constraint — Hard boundaries the code must respect (performance, security, compatibility)
  @gsd-pattern    — Document a reusable pattern established here that should be followed elsewhere
  @gsd-ref        — Cross-reference to another file, doc, issue, or external resource
  @gsd-risk       — Flag known risks, edge cases, or fragile areas
  @gsd-api        — Document a public API surface: contract, parameters, return shape

Stability guarantee to state verbatim: "Tag names and parenthesized keys are stable as of v1.0. New optional metadata keys may be added in future versions. Tag types will not be renamed."

JSON tag object shape (from scanner — document this in arc-standard.md):
  { "type": "context", "file": "src/auth/jwt.js", "line": 12, "metadata": { "phase": "1" }, "description": "...", "raw": "// @gsd-context(phase:1) ..." }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write arc-standard.md</name>
  <files>get-shit-done/references/arc-standard.md</files>
  <read_first>
    - get-shit-done/references/checkpoints.md (existing reference doc — read for formatting conventions used in this directory)
    - get-shit-done/references/verification-patterns.md (second reference doc — confirm section heading style)
    - .planning/phases/01-annotation-foundation/01-CONTEXT.md (decisions D-01 through D-04 and D-13)
    - .planning/phases/01-annotation-foundation/01-RESEARCH.md (arc-standard.md example in Code Examples section; Pitfall 2 about stability guarantee; Comment anchor regex)
  </read_first>
  <action>
Create `get-shit-done/references/arc-standard.md`. The file must contain these sections in this order:

**1. Header block:**
```
# ARC Annotation Standard

**Version:** 1.0
**Stability:** Stable — tag names and parenthesized keys will not change in v1.x. New optional metadata keys may be added in future versions. Tag types will not be renamed.
**Purpose:** @gsd-tags embedded in source code comments are machine-readable planning metadata. They allow the tag scanner to extract CODE-INVENTORY.md and allow agents to understand code structure and intent without reading every file.
```

**2. Section `## Tag Syntax`**
Show the single-line format: `@gsd-<type>[(key:value, key:value)] Description text`
Give three example lines:
- No metadata: `// @gsd-context JWT validation module — stateless, RS256 only`
- Single key: `// @gsd-todo(phase:2) Add refresh token rotation`
- Multiple keys: `// @gsd-todo(phase:2, priority:high) Add refresh token rotation`

**3. Section `## Comment Anchor Rule`**
State: A tag is ONLY valid when it appears on a line where the first non-whitespace content is a comment-opening token.
Valid tokens: `//`, `//+`, `/*`, `*` (block comment continuation line), `#`, `--`, `"""` (Python docstring opener), `'''` (Python docstring opener).
State explicitly: "@gsd- appearing inside a string literal, URL path, or template literal is NOT a tag and the scanner will skip it."
Show side-by-side valid vs invalid:
```
// @gsd-context Valid tag — anchored to comment token     (VALID)
const x = "// @gsd-todo this is NOT a tag";               (INVALID — inside string)
```

**4. Section `## Tag Types`**
Markdown table with columns: Tag | Purpose
All eight tags from D-03 with the purpose descriptions from the interfaces block above.
Then for each tag: a short paragraph (2-3 sentences) explaining when to use it, followed by one realistic example line.

**5. Section `## Metadata Keys`**
Explain: metadata is a parenthesized comma-separated list of `key:value` pairs. Any key is valid. Conventional standard keys:
- `phase:<N>` — project phase this tag applies to (e.g., `phase:2`)
- `priority:<value>` — urgency level (`priority:high`, `priority:medium`, `priority:low`)
- `ref:<id>` — external reference (issue number, requirement ID, PR number)
State: "The scanner stores all metadata as a flat key-value object. Agents interpret keys by convention, not by schema enforcement."

**6. Section `## Language Examples`**
Code blocks for each language using its comment syntax. Each block must show at least 2 different tag types. Use realistic non-toy examples.

JavaScript/TypeScript (`//` comments):
```typescript
// @gsd-context(phase:1) Auth middleware — validates JWT on every protected route
// @gsd-decision Using jose over jsonwebtoken: jose is ESM-compatible, no CommonJS issues
export async function authMiddleware(req, res, next) { ... }
```

Python (`#` and `"""` comments):
```python
# @gsd-constraint No external HTTP calls from this module — must be pure compute
# @gsd-todo(phase:2, priority:high) Add caching layer for repeated signature verifications
def verify_token(token: str) -> dict:
    """
    @gsd-api Parameters: token (str) — raw JWT. Returns: decoded payload dict or raises AuthError.
    """
```

Go (`//` comments):
```go
// @gsd-pattern Use sync.Once for all singleton initializations in this package
// @gsd-risk(ref:ISSUE-142) Race condition possible if Init() called before DB is ready
func Init() *DB { ... }
```

Rust (`//` comments):
```rust
// @gsd-context FFI boundary to the C crypto library — unsafe block intentional
// @gsd-risk Memory safety: caller must ensure buf lives longer than the returned slice
unsafe fn decrypt_raw(buf: *const u8, len: usize) -> &'static [u8] { ... }
```

SQL (`--` comments):
```sql
-- @gsd-context Partitioned by tenant_id for query isolation — max 50K rows per partition
-- @gsd-constraint No cross-tenant JOINs allowed in this view
CREATE VIEW tenant_events AS ...
```

Shell (`#` comments):
```sh
# @gsd-context Bootstraps the dev environment — must be idempotent
# @gsd-todo(phase:3) Add --dry-run flag for CI validation without side effects
set -euo pipefail
```

**7. Section `## What the Scanner Extracts`**
Show the JSON object shape for a single extracted tag:
```json
{
  "type": "context",
  "file": "src/auth/jwt.js",
  "line": 12,
  "metadata": { "phase": "1" },
  "description": "JWT validation module — stateless, RS256 only",
  "raw": "// @gsd-context(phase:1) JWT validation module — stateless, RS256 only"
}
```
Note: `type` is the tag name without the `@gsd-` prefix. `metadata` is `{}` when no parenthesized keys are present. `line` is 1-based.

**8. Section `## GSD Commands`**
One short paragraph: `extract-plan` scans the project and writes `.planning/prototype/CODE-INVENTORY.md` grouped by tag type and file. `annotate` spawns gsd-annotator to retroactively add @gsd-tags to existing code and auto-runs `extract-plan` on completion.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const c = fs.readFileSync('get-shit-done/references/arc-standard.md', 'utf-8');
const checks = [
  '## Tag Syntax', '## Comment Anchor Rule', '## Tag Types', '## Metadata Keys',
  '## Language Examples', '## What the Scanner Extracts', '## GSD Commands',
  '@gsd-context', '@gsd-decision', '@gsd-todo', '@gsd-constraint',
  '@gsd-pattern', '@gsd-ref', '@gsd-risk', '@gsd-api',
  'Version:** 1.0', 'Stable', 'will not be renamed',
  'JavaScript', 'Python', 'Go', 'Rust', 'SQL', 'Shell',
  'phase:', 'priority:', 'VALID', 'INVALID'
];
const missing = checks.filter(k => !c.includes(k));
if (missing.length) { console.error('MISSING:', missing); process.exit(1); }
console.log('OK — arc-standard.md has all required sections');
"</automated>
  </verify>
  <acceptance_criteria>
    - `get-shit-done/references/arc-standard.md` exists
    - File contains `**Version:** 1.0` and `will not be renamed`
    - File contains all 8 tag type names: `@gsd-context`, `@gsd-decision`, `@gsd-todo`, `@gsd-constraint`, `@gsd-pattern`, `@gsd-ref`, `@gsd-risk`, `@gsd-api`
    - File contains sections: `## Tag Syntax`, `## Comment Anchor Rule`, `## Tag Types`, `## Metadata Keys`, `## Language Examples`, `## What the Scanner Extracts`, `## GSD Commands`
    - File contains language examples for: JavaScript, Python, Go, Rust, SQL, Shell
    - File contains `VALID` and `INVALID` examples in Comment Anchor Rule section
    - File contains the JSON object shape showing `type`, `file`, `line`, `metadata`, `description`, `raw` fields
  </acceptance_criteria>
  <done>arc-standard.md exists at get-shit-done/references/arc-standard.md with version 1.0, all 8 tag types defined, comment-anchor rule explained, 6 language examples, and the scanner JSON schema documented</done>
</task>

</tasks>

<verification>
node -e "
const fs = require('fs');
const c = fs.readFileSync('get-shit-done/references/arc-standard.md', 'utf-8');
const required = ['Version:** 1.0', '## Tag Syntax', '## Comment Anchor Rule', '@gsd-context', '@gsd-decision', '@gsd-todo', '@gsd-constraint', '@gsd-pattern', '@gsd-ref', '@gsd-risk', '@gsd-api', 'JavaScript', 'Python', 'Rust', 'SQL', 'Shell'];
const missing = required.filter(s => !c.includes(s));
if (missing.length) process.exit(1);
console.log('Phase 1 Plan 01 verification: PASS');
"
</verification>

<success_criteria>
`get-shit-done/references/arc-standard.md` exists, contains version 1.0 with stability guarantee, defines all 8 tag types with purpose and example, documents the comment-anchor rule with valid/invalid examples, shows language examples for JS/Python/Go/Rust/SQL/Shell, and documents the JSON shape the scanner produces.
</success_criteria>

<output>
After completion, create `.planning/phases/01-annotation-foundation/01-01-SUMMARY.md`
</output>
