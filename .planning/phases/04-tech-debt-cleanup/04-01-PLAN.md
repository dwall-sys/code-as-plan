---
phase: 04-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/annotate.md
  - commands/gsd/prototype.md
  - commands/gsd/extract-plan.md
  - agents/gsd-annotator.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "annotate.md, prototype.md, extract-plan.md contain no <execution_context> blocks"
    - "gsd-annotator passes all agent-frontmatter tests including anti-heredoc and hooks checks"
  artifacts:
    - path: "commands/gsd/annotate.md"
      provides: "annotate command without stale workflow ref"
      contains: "<process>"
    - path: "commands/gsd/prototype.md"
      provides: "prototype command without stale workflow ref"
      contains: "<process>"
    - path: "commands/gsd/extract-plan.md"
      provides: "extract-plan command without stale workflow ref"
      contains: "<process>"
    - path: "agents/gsd-annotator.md"
      provides: "annotator agent passing all frontmatter tests"
      contains: "# hooks:"
  key_links:
    - from: "agents/gsd-annotator.md"
      to: "tests/agent-frontmatter.test.cjs"
      via: "test assertions for file-writing agent patterns"
      pattern: "never use .Bash\\(cat << 'EOF'\\). or heredoc"
---

<objective>
Remove stale workflow references from 3 command files and fix gsd-annotator agent to pass all frontmatter tests.

Purpose: Close 4 of 5 tech debt items from v1.0-MILESTONE-AUDIT.md -- stale <execution_context> blocks that reference non-existent workflow files, and 2 failing agent-frontmatter tests for gsd-annotator.
Output: 4 clean Markdown files; 0 test failures in agent-frontmatter.test.cjs
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tech-debt-cleanup/04-RESEARCH.md

<interfaces>
<!-- Reference implementations for gsd-annotator fix -->

From agents/gsd-prototyper.md (lines 7-12, inside frontmatter):
```yaml
# hooks:
#   PostToolUse:
#     - matcher: "Write|Edit"
#       hooks:
#         - type: command
#           command: "npx eslint --fix $FILE 2>/dev/null || true"
```

From agents/gsd-prototyper.md (line 18, inside <role> section):
```markdown
**ALWAYS use the Write tool to create files** -- never use `Bash(cat << 'EOF')` or heredoc commands for file creation.
```

Test assertion exact strings (from tests/agent-frontmatter.test.cjs):
- Hooks check: `frontmatter.includes('# hooks:')` where frontmatter = content.split('---')[1]
- Anti-heredoc check: `content.includes("never use \`Bash(cat << 'EOF')\` or heredoc")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale execution_context blocks from 3 command files</name>
  <files>commands/gsd/annotate.md, commands/gsd/prototype.md, commands/gsd/extract-plan.md</files>
  <read_first>
    - commands/gsd/annotate.md
    - commands/gsd/prototype.md
    - commands/gsd/extract-plan.md
  </read_first>
  <action>
Remove the entire `<execution_context>...</execution_context>` block (including opening tag, content line, and closing tag) from each of the 3 files:

1. **commands/gsd/annotate.md** — Remove lines 25-27:
   ```
   <execution_context>
   @~/.claude/get-shit-done/workflows/annotate.md
   </execution_context>
   ```

2. **commands/gsd/prototype.md** — Remove lines 25-27:
   ```
   <execution_context>
   @~/.claude/get-shit-done/workflows/prototype.md
   </execution_context>
   ```

3. **commands/gsd/extract-plan.md** — Remove lines 20-22:
   ```
   <execution_context>
   @~/.claude/get-shit-done/workflows/extract-plan.md
   </execution_context>
   ```

Do NOT modify `<process>` sections or `<context>` sections -- those are the working execution logic. Only remove the vestigial `<execution_context>` blocks.

Also remove any blank line left behind where the block was, so there is exactly one blank line between the preceding section and the following section.
  </action>
  <verify>
    <automated>grep -rn 'execution_context' commands/gsd/annotate.md commands/gsd/prototype.md commands/gsd/extract-plan.md; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'execution_context' commands/gsd/annotate.md` returns 0
    - `grep -c 'execution_context' commands/gsd/prototype.md` returns 0
    - `grep -c 'execution_context' commands/gsd/extract-plan.md` returns 0
    - `grep -c '<process>' commands/gsd/annotate.md` returns 1 (process section preserved)
    - `grep -c '<process>' commands/gsd/prototype.md` returns 1 (process section preserved)
    - `grep -c '<process>' commands/gsd/extract-plan.md` returns 1 (process section preserved)
  </acceptance_criteria>
  <done>All 3 command files have no execution_context blocks and retain their process sections intact</done>
</task>

<task type="auto">
  <name>Task 2: Fix gsd-annotator agent frontmatter to pass all tests</name>
  <files>agents/gsd-annotator.md</files>
  <read_first>
    - agents/gsd-annotator.md
    - agents/gsd-prototyper.md
    - tests/agent-frontmatter.test.cjs
  </read_first>
  <action>
Two changes to agents/gsd-annotator.md:

**Change 1: Add commented hooks block to frontmatter.**
Insert the following block after `color: green` (line 6) and before the closing `---` (currently line 7). Copy verbatim from gsd-prototyper.md lines 7-12:

```yaml
# hooks:
#   PostToolUse:
#     - matcher: "Write|Edit"
#       hooks:
#         - type: command
#           command: "npx eslint --fix $FILE 2>/dev/null || true"
```

The frontmatter section (between first `---` and second `---`) must contain `# hooks:` for the test to pass. The test checks `content.split('---')[1].includes('# hooks:')`.

**Change 2: Add anti-heredoc instruction to the role section.**
In the `<role>` section, after the existing "CRITICAL: Do not change any existing code logic..." paragraph, add a new paragraph with this exact text:

```
**ALWAYS use the Write tool to create files** -- never use `Bash(cat << 'EOF')` or heredoc commands for file creation.
```

The test checks for the exact substring: `never use \`Bash(cat << 'EOF')\` or heredoc` — the backticks around `Bash(cat << 'EOF')` are literal markdown backticks in the file content.

Do NOT modify any other part of the agent file. Do NOT modify the test file.
  </action>
  <verify>
    <automated>node --test tests/agent-frontmatter.test.cjs 2>&1 | grep -E "(fail|gsd-annotator)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/agent-frontmatter.test.cjs 2>&1 | grep "fail 0"` returns a match
    - `grep -c "# hooks:" agents/gsd-annotator.md` returns 1
    - `grep -c "never use" agents/gsd-annotator.md` returns 1
    - The `# hooks:` text appears between the first and second `---` lines (in frontmatter, not body)
  </acceptance_criteria>
  <done>gsd-annotator passes all agent-frontmatter tests with 0 failures</done>
</task>

</tasks>

<verification>
1. `grep -rn 'execution_context' commands/gsd/annotate.md commands/gsd/prototype.md commands/gsd/extract-plan.md` — returns nothing
2. `node --test tests/agent-frontmatter.test.cjs` — 0 failures
3. All 3 command files still have their `<process>` sections intact
</verification>

<success_criteria>
- Zero `<execution_context>` blocks in annotate.md, prototype.md, extract-plan.md
- Zero test failures in agent-frontmatter.test.cjs
- No changes to any .cjs runtime files or test files
</success_criteria>

<output>
After completion, create `.planning/phases/04-tech-debt-cleanup/04-01-SUMMARY.md`
</output>
