---
phase: 04-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "README.md documents that ARC wrapper agents are only reachable via /gsd:iterate, not via /gsd:execute-phase or /gsd:plan-phase"
    - "Known Limitations section is inside the fork content area, before the upstream separator"
  artifacts:
    - path: "README.md"
      provides: "Known Limitations documentation for ARC routing"
      contains: "Known Limitations"
  key_links:
    - from: "README.md"
      to: "User understanding of ARC routing limitation"
      via: "Known Limitations section"
      pattern: "Only Reachable via.*iterate"
---

<objective>
Add a Known Limitations section to README.md documenting that ARC wrapper agents are only reachable via /gsd:iterate.

Purpose: Close the final tech debt item from v1.0-MILESTONE-AUDIT.md -- users need to know that arc.enabled does not affect /gsd:execute-phase or /gsd:plan-phase routing.
Output: README.md with a Known Limitations section in the fork content area
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tech-debt-cleanup/04-RESEARCH.md

<interfaces>
<!-- README.md fork section structure -->

The README has a fork section prepended before upstream content. The boundary is marked by:
- Line 69: `---`
- Line 71: `*The sections below are from the original GSD framework...*`
- Line 73: `---`
- Line 75+: Upstream content starting with `<div align="center">`

The Known Limitations section must go BEFORE line 69 (the first `---` separator),
after the "Code-First Commands" table which ends at line 67.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Known Limitations section to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/phases/04-tech-debt-cleanup/04-RESEARCH.md
  </read_first>
  <action>
Insert a new section in README.md between the "Code-First Commands" table (which ends with "All original GSD commands continue working unchanged." on line 67) and the first `---` separator (line 69).

Insert the following content after line 67 ("All original GSD commands continue working unchanged."), with one blank line before and after:

```markdown

## Known Limitations

### ARC Wrapper Agents: Only Reachable via `/gsd:iterate`

When `arc.enabled` is `true` in your project config, the ARC wrapper agents
(`gsd-arc-executor` and `gsd-arc-planner`) are only invoked by the
`/gsd:iterate` command. The traditional GSD entry points `/gsd:execute-phase`
and `/gsd:plan-phase` always spawn the standard `gsd-executor` and
`gsd-planner` regardless of the `arc.enabled` setting.

**Workaround:** Use `/gsd:iterate` for code-first workflows where ARC
annotation obligations are needed. Use `/gsd:execute-phase` or
`/gsd:plan-phase` for plan-first workflows where ARC obligations are not
required.

This limitation is tracked for resolution in v1.1.

```

This places the section inside the fork content area, before the `---` that separates fork from upstream. It does NOT modify any upstream content below the separator.

The exact text above is specified in 04-RESEARCH.md and matches the phase success criteria in ROADMAP.md.
  </action>
  <verify>
    <automated>grep -n "Known Limitations" README.md && grep -n "Only Reachable via" README.md && echo "---" && grep -n "sections below are from the original" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Known Limitations" README.md` returns 1
    - `grep -c "Only Reachable via" README.md` returns 1
    - `grep -c "gsd-arc-executor" README.md` returns at least 1
    - `grep -c "tracked for resolution in v1.1" README.md` returns 1
    - The "Known Limitations" line number is LESS than the "sections below are from the original" line number (fork area, not upstream area)
    - `grep -c "Code-First Commands" README.md` returns 1 (preceding section preserved)
  </acceptance_criteria>
  <done>README.md contains Known Limitations section documenting ARC routing constraint, placed in fork content area before upstream separator</done>
</task>

</tasks>

<verification>
1. `grep "Known Limitations" README.md` — returns match
2. `grep "Only Reachable via" README.md` — returns match
3. Known Limitations section appears before the `---` separator and upstream content
4. No upstream content below the separator was modified
</verification>

<success_criteria>
- README.md has a "Known Limitations" section visible to fork users
- Section documents that ARC wrapper agents are only reachable via /gsd:iterate
- Section is in the fork content area (before upstream separator)
- Upstream README content is untouched
</success_criteria>

<output>
After completion, create `.planning/phases/04-tech-debt-cleanup/04-02-SUMMARY.md`
</output>
