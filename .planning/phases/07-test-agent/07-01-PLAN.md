---
phase: 07-test-agent
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/test-detector.cjs
  - tests/test-detector.test.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "detectTestFramework() returns correct framework for vitest, jest, mocha, ava, and node:test projects"
    - "detectTestFramework() falls back to node:test when no package.json exists or no framework is recognized"
    - "gsd-tools detect-test-framework subcommand outputs JSON with framework, testCommand, and filePattern fields"
  artifacts:
    - path: "get-shit-done/bin/lib/test-detector.cjs"
      provides: "Test framework auto-detection from package.json"
      exports: ["detectTestFramework"]
    - path: "tests/test-detector.test.cjs"
      provides: "Unit tests for test-detector module"
      min_lines: 60
    - path: "get-shit-done/bin/gsd-tools.cjs"
      provides: "detect-test-framework subcommand"
      contains: "case 'detect-test-framework'"
  key_links:
    - from: "get-shit-done/bin/gsd-tools.cjs"
      to: "get-shit-done/bin/lib/test-detector.cjs"
      via: "require('./lib/test-detector.cjs')"
      pattern: "require.*test-detector"
    - from: "tests/test-detector.test.cjs"
      to: "get-shit-done/bin/lib/test-detector.cjs"
      via: "require('../get-shit-done/bin/lib/test-detector.cjs')"
      pattern: "require.*test-detector"
---

<objective>
Create the test-detector.cjs utility module that auto-detects a project's test framework from package.json, with full TDD test coverage and a gsd-tools.cjs subcommand for agent access.

Purpose: The gsd-tester agent (Plan 02) needs to know which test framework a target project uses before writing tests. This module provides deterministic detection via package.json inspection.
Output: test-detector.cjs module, test file, gsd-tools.cjs subcommand
</objective>

<execution_context>
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/workflows/execute-plan.md
@/Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- CJS module pattern from config.cjs -->
From get-shit-done/bin/lib/config.cjs:
```javascript
const fs = require('fs');
const path = require('path');
const { output, error, planningRoot } = require('./core.cjs');
// ... module.exports = { ... };
```

<!-- gsd-tools.cjs subcommand pattern (extract-tags reference) -->
From get-shit-done/bin/gsd-tools.cjs (lines 914-928):
```javascript
case 'extract-tags': {
  const allArgs = args.slice(1);
  // ... parsing ...
  arcScanner.cmdExtractTags(cwd, targetPath, { ... });
  break;
}
```

<!-- gsd-tools.cjs output helper -->
From get-shit-done/bin/gsd-tools.cjs:
```javascript
// core.output(data, raw, humanFriendlyString)
// When raw=true: outputs JSON. When raw=false: outputs humanFriendlyString
```

<!-- Test helpers pattern -->
From tests/helpers.cjs:
```javascript
const { execSync, execFileSync } = require('child_process');
function runGsdTools(args, cwd = process.cwd(), env = {}) { ... }
// Returns { success: true, output: result.trim() } or { success: false, output, error }
```

<!-- End of switch block where new subcommand goes -->
From get-shit-done/bin/gsd-tools.cjs (lines 943-947):
```javascript
    // line 943: } (end of set-mode case)
    // line 944: (blank)
    // line 945: default:
    // line 946:   error(`Unknown command: ${command}`);
    // line 947: }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test-detector.cjs and its tests (TDD)</name>
  <files>get-shit-done/bin/lib/test-detector.cjs, tests/test-detector.test.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/config.cjs (CJS module pattern: require style, module.exports, error handling)
    - tests/config.test.cjs (node:test pattern: describe/test/assert, helpers usage)
    - tests/helpers.cjs (createTempProject, cleanup, runGsdTools helpers)
    - .planning/phases/07-test-agent/07-RESEARCH.md (detection algorithm, code examples)
  </read_first>
  <behavior>
    - Test 1: Returns { framework: 'node:test', testCommand: 'node --test', filePattern: '**/*.test.cjs' } when package.json does not exist at projectRoot
    - Test 2: Returns { framework: 'node:test', ... } when package.json has invalid JSON
    - Test 3: Returns { framework: 'vitest', testCommand: 'npx vitest run', filePattern: '**/*.test.{ts,js}' } when devDependencies contains 'vitest'
    - Test 4: Returns { framework: 'vitest', ... } when scripts.test contains 'vitest'
    - Test 5: Returns { framework: 'jest', testCommand: 'npx jest', filePattern: '**/*.test.{ts,js}' } when devDependencies contains 'jest'
    - Test 6: Returns { framework: 'jest', ... } when scripts.test contains 'jest'
    - Test 7: Returns { framework: 'mocha', testCommand: 'npx mocha', filePattern: '**/*.test.{mjs,cjs,js}' } when devDependencies contains 'mocha'
    - Test 8: Returns { framework: 'ava', testCommand: 'npx ava', filePattern: '**/*.test.{mjs,js}' } when devDependencies contains 'ava'
    - Test 9: Returns { framework: 'node:test', ... } when scripts.test contains '--test' (e.g., 'node --test')
    - Test 10: Returns { framework: 'node:test', ... } when package.json exists but has no test framework in deps or scripts
    - Test 11: Priority order — vitest wins over jest when both are in devDependencies
  </behavior>
  <action>
    RED phase first: Write tests/test-detector.test.cjs with all 11 behavior cases using node:test (require('node:test')) and node:assert. Use fs.mkdtempSync to create temp directories with custom package.json files for each test case. Each test writes a package.json to a temp dir, calls detectTestFramework(tmpDir), and asserts the result object.

    Then GREEN phase: Create get-shit-done/bin/lib/test-detector.cjs following the exact algorithm from RESEARCH.md code example:
    - 'use strict' at top
    - require('fs') and require('path') only — zero external deps (per D-06)
    - Export single function: detectTestFramework(projectRoot)
    - Detection priority: vitest > jest > mocha > ava > node:test (scripts.test --test) > node:test (fallback)
    - Each return value is { framework: string, testCommand: string, filePattern: string }
    - Wrap JSON.parse in try/catch — return node:test fallback on parse error

    The module does NOT import core.cjs — it is a pure utility. The gsd-tools.cjs subcommand (Task 2) handles output formatting.
  </action>
  <verify>
    <automated>cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node --test tests/test-detector.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File get-shit-done/bin/lib/test-detector.cjs exists and contains `module.exports = { detectTestFramework }`
    - File tests/test-detector.test.cjs exists and contains `require('node:test')` and `require('node:assert')`
    - All 11 test cases pass with `node --test tests/test-detector.test.cjs`
    - No external dependencies — only require('fs') and require('path') in test-detector.cjs
    - Function signature is detectTestFramework(projectRoot) returning { framework, testCommand, filePattern }
  </acceptance_criteria>
  <done>detectTestFramework returns correct framework for all 5 supported frameworks, falls back to node:test for missing/invalid/unrecognized package.json, and all tests pass green</done>
</task>

<task type="auto">
  <name>Task 2: Add detect-test-framework subcommand to gsd-tools.cjs</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    - get-shit-done/bin/gsd-tools.cjs (lines 910-950 — extract-tags case and default case for placement reference)
    - get-shit-done/bin/lib/test-detector.cjs (the module created in Task 1)
  </read_first>
  <action>
    Add a new case block to the main switch statement in gsd-tools.cjs, BEFORE the `default:` case (which is at line 945). Insert it after the `set-mode` case block (line 943).

    The new case block:
    ```javascript
    case 'detect-test-framework': {
      const targetDir = args[1] || cwd;
      const testDetector = require('./lib/test-detector.cjs');
      const result = testDetector.detectTestFramework(targetDir);
      core.output(result, raw, `${result.framework}: ${result.testCommand}`);
      break;
    }
    ```

    This follows the exact pattern of existing subcommands:
    - args[1] is the optional projectRoot argument, defaults to cwd
    - require() the lib module inline (same as extract-tags does with arcScanner)
    - core.output() for JSON (--raw) or human-readable output

    Also add `detect-test-framework <dir>` to the help text comment block at the top of the file (lines 1-30), after the existing subcommand list.
  </action>
  <verify>
    <automated>cd /Users/denniswall/Desktop/GSD-Code-FIrst/gsd-code-first && node get-shit-done/bin/gsd-tools.cjs detect-test-framework --raw | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(d.framework); process.exit(d.framework && d.testCommand && d.filePattern ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - gsd-tools.cjs contains `case 'detect-test-framework':` before `default:`
    - Running `node gsd-tools.cjs detect-test-framework --raw` outputs valid JSON with framework, testCommand, and filePattern keys
    - Running `node gsd-tools.cjs detect-test-framework` (without --raw) outputs human-readable string like "vitest: npx vitest run"
    - Running `node gsd-tools.cjs detect-test-framework /nonexistent/path --raw` outputs JSON with framework="node:test" (fallback)
    - Existing tests still pass: `node --test tests/gsd-tools.test.cjs` exits 0
  </acceptance_criteria>
  <done>detect-test-framework subcommand registered in gsd-tools.cjs, outputs correct JSON for the current project, falls back to node:test for nonexistent paths, existing tests unbroken</done>
</task>

</tasks>

<verification>
1. `node --test tests/test-detector.test.cjs` — all 11 test cases pass
2. `node get-shit-done/bin/gsd-tools.cjs detect-test-framework --raw` — returns valid JSON with framework field
3. `node get-shit-done/bin/gsd-tools.cjs detect-test-framework /tmp --raw` — returns node:test fallback
4. `npm test 2>&1 | tail -5` — existing test suite still passes
</verification>

<success_criteria>
- test-detector.cjs is a zero-dependency CJS module exporting detectTestFramework()
- All 5 frameworks (vitest, jest, mocha, ava, node:test) are correctly detected from package.json
- gsd-tools.cjs exposes detect-test-framework subcommand for agent consumption
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-test-agent/07-01-SUMMARY.md`
</output>
